name = "frosteditor-collaboration"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Durable Objects 配置
[durable_objects]
bindings = [
  { name = "ROOM", class_name = "RoomObject" }
]

# Durable Objects 迁移（首次部署需要）
[[migrations]]
tag = "v1"
new_sqlite_classes = ["RoomObject"]

# 开发服务器配置
[dev]
port = 8765
