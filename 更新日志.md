# 更新日志（本 Fork 定制）

本文件记录基于 TurboWarp scratch-gui 的**定制增强**（中文界面版）。上游原版更新见 `CHANGELOG.md`。

> 所有改动须 `npm start` **完整重启**后生效——HMR 热更新不会重新初始化 reducer 新增字段，可能导致新功能不生效。

---

## 2026-07-26

### 新增：代码锁定模式（`?lock`）

防止作品源码被随意查看。远程作品 URL 携带 `?lock` 参数即标记为「代码锁定」：

- **embed 播放页**（`/embed.html`）自动隐藏「在编辑器中打开」按钮，访客无法跳转到编辑器查看源码（默认不锁定，按钮照常显示）。
- **编辑器开发页被强开时拦截**：当有人**顶层直接打开** `/editor.html`（或路径形式 `/xxx/editor`）且非 iframe 嵌入、加载了锁定作品时，弹出警告「你个18码的艾斯贼，不要盗源码！」，并强制退回**根路径播放页**（去掉 URL 中的 `editor.html` 段，例如 `http://域名/?project_url=...&lock`）。
- **不误伤正常场景**：根路径 `/?project_url=...` 视为「分享/播放页」不弹窗；通过 `<iframe>` 嵌入任意路径（`window.parent !== window`）也不弹窗，作者在自己网站嵌入展示不受影响。
- **参数写法**（任选其一，效果相同）：
  - 挂在远程作品 URL 后：`?=https://...sb3?lock`
  - 挂在嵌入页 URL 作兄弟参数：`?=https://...sb3&lock`
  - 标准写法：`?project_url=https://...sb3?lock`
- 涉及文件：`src/reducers/tw.js`、`src/lib/project-fetcher-hoc.jsx`、`src/components/stage-header/stage-header.jsx`、`src/components/tw-code-lock-warning.jsx`、`src/components/gui/gui.jsx`。

### 新增：embed 模式「在编辑器中打开」按钮

- embed 播放页（`/embed.html`）舞台顶部新增「在编辑器中打开」按钮，点击打开完整编辑器并保留 `project_url`，方便从播放页进入编辑。
- 在 iframe 内通过 `window.open` 跳出，避免被 iframe 拦截。
- 涉及文件：`src/components/stage-header/stage-header.jsx`、`src/components/stage-header/icon--open-editor.svg`。

### 新增：跨域 CORS 代理（`/proxy`）

- 新增同源 `/proxy` 代理，使任意域名的 `.sb3` 作品可被加载，规避浏览器跨域（CORS）限制。
- 跨域作品 URL 经同源代理请求并回传，响应头 `Access-Control-Allow-Origin: *`。
- 含 SSRF 防护（拦截回环/内网地址）、重定向上限、体积（200MB）与超时（30s）限制；远程返回 HTML 时返回 502 错误页。
- 开发环境：挂载到 `webpack.config.js` 的 `devServer.before`；生产环境：独立运行 `proxy-server.js`（默认监听 8602），或由 nginx 反代 `/proxy`。
- 涉及文件：`src/lib/tw-cors-proxy.js`、`webpack.config.js`、`proxy-server.js`、`src/lib/project-fetcher-hoc.jsx`。

---

## 备注

- 远程作品加载支持两种来源形式：`?project_url=https://...`（标准 TurboWarp 形式）与 `?=https://...`（无参数名简写形式）。
- 跨域加载走 `/proxy` 代理；同域或 `data:` 协议不代理，避免死循环。
- 素材级跨域（角色造型/声音来自外部域名）如需支持，可进一步在 storage 层增加代理。
